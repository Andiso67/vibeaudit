# GitPython: clonar repositorios y manipular historial de git desde Python
gitpython==3.1.40

# Pydantic: modelos de datos con validación de tipos en tiempo de ejecución
pydantic==2.13.4

# Typer: construcción de interfaces de línea de comandos (CLI)
typer==0.9.0

# Rich: salida de terminal con formato (tablas, colores, barras de progreso)
rich==13.7.0

# PyYAML: parseo y serialización de archivos YAML (configuración y resultados)
pyyaml==6.0.1

# Httpx: cliente HTTP para el motor LLM (API compatible OpenAI, p. ej. Ollama)
httpx==0.28.1

# ReportLab: generación del entregable PDF one-page (métricas, semáforo y top hallazgos)
reportlab==5.0.0

# Psycopg 3: persistencia de análisis en PostgreSQL (opcional; API sin BD si no hay DATABASE_URL)
psycopg[binary]==3.2.13

# FastAPI + Uvicorn: servicio HTTP de la API
fastapi==0.128.8
uvicorn==0.39.0

# Pytest: framework de tests unitarios
pytest==8.3.4
